[project]
name = "shabti"
version = "0.1.0"
description = "A helper for infra related tasks."
readme = "README.md"
license = "Apache-2.0"
requires-python = ">=3.13"
dependencies = ["click>=8.2.1"]

[tool]
uv.package = true

[project.scripts]
sb = "main:cli"
